class Bound {
    int top, bottom, left, right;

    Bound(int top, int right, int bottom, int left) {
        this.bottom = bottom;
        this.top = top;
        this.left = left;
        this.right = right;
    }
}
